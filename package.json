{
  "name": "utilityserver",
  "version": "1.0.0",
  "description": "server that takes in different routes to do random but COOL things!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pamela Abreu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "request": "^2.88.0"
  }
}
